## Setup

```sh
# Install dependencies
brew install gh
brew install jq

# Make executable
chmod +x *.sh

# Create a GitHub token
gh auth login
```

or just run setup.sh

```sh
chmod +x setup.sh
./setup.sh
```
